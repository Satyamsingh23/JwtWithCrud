import { TeacherDirectiveDirective } from './teacher-directive.directive';

describe('TeacherDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TeacherDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
